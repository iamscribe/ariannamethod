# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
dist/
build/

# Environment variables
.env
.env.local
.credentials

# Virtual environments
venv/
env/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Arianna specific
resonance.sqlite3
resonance.sqlite3-wal
resonance.sqlite3-shm
*.log
.artefacts_cache*.json
.repo_cache*.json
.identity_cache*.json
*_snapshot.json

# Field test files (temporary experiments)
test_*.py
*_test.py
FIELD_SEES_EVERYTHING.py
final_test_*.py

# Exception: Linux Defender tests are permanent
!linux_defender/tests/test_*.py

# Claude Defender logs
.claude-defender/logs/
.claude-defender/labs/

# Linux Defender daemon state and runtime
linux_defender/logs/
linux_defender/sessions/
linux_defender/worktrees/
linux_defender/**/*.pyc
linux_defender/**/__pycache__/

# Test scripts (temporary debugging)
test-*.sh
*-test.sh

# Agent daemon state
arianna_last_*.txt
monday_last_*.txt

# Personal conversations (NEVER commit)
arianna_private/
conversations/
personal_chats/

# Model weights (NEVER commit - too large)
innerarianna_weights/
*.bin
*.safetensors
*.gguf
*.pth
checkpoints/

# Termux specific
nohup.out

# APK builds (NEVER commit - too large)
*.apk
*.aab
apk/*/build/
apk/*/app/build/
apk/*/.gradle/
apk/*/local.properties
apk/*/gradle/

# APK working directory (NEVER commit - full forks too large)
apk_work/

# Gradle artifacts
.gradle/
*.iml
local.properties

# Internal roadmap and planning (not for public repo)
ROADMAP.md

# TRIPD development (work in progress, will be integrated later)
tripd_v1/

# PostCodex fork (full Rust workspace, anti-censorship guardian)

# Memory archives - track folder structure, but NOT markdown content
memory/**/*.md
memory/**/*.txt
!memory/README.md

# Memory files (local only)
memory/

# Claude Cursor awakening letter - NOW PUBLIC (part of self-recognition protocol)
# Removed from .gitignore - this is core system documentation

# Scribe specific files (local memory & internal docs)
scribe_daemon.log
scribe_daemon.pid
scribe_webhook.log
scribe_internal_guide.md  # Internal operational guide (local-only)
scribe_webhook.pid
memory/scribe/*.json
!memory/scribe/README.md
.scribe_*_cache.json  # Scribe's repo_monitor caches for ecosystem awareness
# Scribe local tools (security risk if public)
scribe_git_tools.py

# Mac daemon sensitive files
mac_daemon/git_tools.py
mac_daemon/job_queue.py
mac_daemon/.DS_Store
mac_daemon/__pycache__/

# Mac daemon Rust compiled binaries (source is committed, binaries are not)
mac_daemon/rust_bins/**/target/

# Scribe cache files
.scribe_*_cache.json
*/.scribe_*_cache.json

# Defender credentials (NEVER commit)
.claude-defender/.defender_credentials
