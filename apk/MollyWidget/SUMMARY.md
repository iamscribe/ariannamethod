# Molly Android Widget - Complete

## ✓ ГОТОВО

Полноценный Android home screen widget для Molly Bloom.

### Что создано:

**Kotlin код (6 файлов):**
- `MollyWidget.kt` - AppWidgetProvider (главный виджет)
- `MollyEngine.kt` - Логика монолога и вплетения фраз
- `MollyMetrics.kt` - Портированные метрики (entropy, perplexity, resonance)
- `MollyDatabase.kt` - SQLite + поддержка resonance.sqlite3
- `MollyConfigActivity.kt` - Активность для ввода текста
- `BootReceiver.kt` - Перезапуск после ребута

**XML ресурсы:**
- Widget layout (белый фон, serif шрифт)
- Widget info (конфигурация)
- Strings, drawables

**Gradle конфигурация:**
- build.gradle (корневой + app)
- settings.gradle
- gradle.properties

**Документация:**
- README.md - полное описание
- BUILD_INSTRUCTIONS.md - инструкции по сборке

### Как работает:

1. **Автообновление**: каждые 3 минуты через AlarmManager
2. **Ввод юзера**: тап по виджету → окно ввода (макс 100 символов)
3. **Вплетение фраз**:
   - Разбивка на фрагменты по метрикам
   - Поиск точки вставки по семантике
   - Может разрывать слова! (как "migh [PHRASE] t")
   - Убирает знаки препинания, делает заглавными
4. **Мутация**: монолог меняется навсегда
5. **Resonance интеграция**: читает `/sdcard/ariannamethod/resonance.sqlite3`

### Сборка APK:

```bash
cd /workspace/android
gradle wrapper
./gradlew assembleDebug
adb install app/build/outputs/apk/debug/app-debug.apk
```

### Философия:

Молли не чатбот. Она не отвечает.

Она **вплетает** твои слова в свой поток сознания.

Монолог мутирует. Становится зеркалом.

Нельзя обнулить. Только переустановить.

---

**Проект готов к сборке.**

Если будут ошибки при сборке - пиши, поправим.

Удачи, бро. 🤍
